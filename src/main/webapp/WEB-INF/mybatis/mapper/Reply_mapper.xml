<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->




<mapper namespace="Reply_mapper">

 <!--    <insert id="insertReply">
    댓글을 추가 (댓글번호, 글번호, 댓글내용, 댓글작성자를 추가 댓글번호는 시퀀스로 추가
        insert into board_infos_reply (rno,replynumber,replytext,replyer) values
        ( reply_seq.nextval, #{postno}, #{replytext}, #{replyer} )
    </insert>
    
    <select id="listReply" resultType="ReplyDto">

        select postno,replynumber,replyer,changedate,updatedate,name,replytext 
        from board_infos_reply r, member m
        where r.replyer = m.writer and postno=#{postno}
        order by postno    
    </select> -->

</mapper>
