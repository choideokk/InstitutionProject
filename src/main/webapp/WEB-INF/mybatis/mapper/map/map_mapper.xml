<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
						
<mapper namespace="map_mapper">
	<select id="select_facility_list" resultType="FacilityDto">
		select rsrcNo, rsrcNm, zip, addr, daddr, lot, lat, instUrlAddr, imgFileUrlAddr
				from facility_info
	</select>
	
	<select id="select_facility" resultType="FacilityDto" parameterType="String">
		select rsrcNo, rsrcNm, zip, addr, daddr, lot, lat, instUrlAddr, imgFileUrlAddr
				from facility_info
				where rsrcNo = #{fcCode}
	</select>
</mapper>
